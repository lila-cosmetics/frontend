import { CreateApi, createApi, fetchBaseQuery } from "@reduxjs/toolkit/query";

//fetchBaseQuery is a function allows us to make req to backend api
import { BASE_URL } from "../api/productsAPI";

export const apiSlice = createApi({
    
})